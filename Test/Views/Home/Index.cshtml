@model IEnumerable<Employee>
@{ 
    //Layout = "~/Views/Shared/_layout.cshtml";
    ViewBag.title = "Index";
}

@*<table>
    <thead>
        <tr>
            <th>Id</th>
            <th>Name</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var emp in Model)
        {
            <tr>
                <td>@emp.Id</td>
                <td>@emp.Name</td>
            </tr>
        }
    </tbody>
</table>*@

<div class="card-deck">
    @foreach (var emp in Model )
    {
       var ImagePath = "~/Images/" + (emp.FilePath ?? "Men.png");
        <div class="card m-3" style="min-width:18rem;max-width:30.5%">
            <div class="card-header">
                <h3>@emp.Name</h3>
            </div>
            @*<img asp-append-version="true" class="card-img-top  ImageThublail" src="@ImagePath" />*@
            <div class="card-footer text-center">
                <a class="btn btn-primary m-1" asp-controller="Home" asp-action="details" asp-route-id="@emp.Id">View</a>
                @*<a href="@Url.Action("details","Home", new {id=emp.Id })" class="btn btn-primary">View</a>*@
                @*<a h,ref="/home/details/@emp.Id" class="btn btn-primary">View</a>*@
                <a asp-controller="home" asp-action="Edit" asp-route-id="@emp.Id" class="btn btn-primary m-1">Edit</a>
                <a href="#" class="btn btn-danger m-1">Delete</a>

            </div>
        </div>
    }

</div>
